
import Koa                  from 'koa'
import koaLogger            from 'koa-logger'
import koaSession           from 'koa-session'
import router               from './routes'

const app = new Koa();


export default app;
